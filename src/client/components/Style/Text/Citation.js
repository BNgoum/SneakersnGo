import styled from 'styled-components';

const CitationStyle = styled.Text`
    font-size: 32px;
    text-transform: uppercase;
    font-family: 'roboto-bold';
    letter-spacing: 1px;
`;

export default CitationStyle;